# participant-profile requirements matrix

> Requirement matrix for the safety.viz **participant-profile** module — the standardized-lab participant drill-down (header / labs-over-time spaghetti / measure table with sparkline insets) extracted from the original [SafetyGraphics/hep-explorer](https://github.com/SafetyGraphics/hep-explorer) Details View into a chart-agnostic module with two mounts: standalone (`SafetyViz.participantProfile`) and docked below a host chart. Rows are authored from the nine issue-level requirements PPRF-1..PPRF-9 of [safety.viz#98](https://github.com/jwildfire/safety.viz/issues/98) and the rollout requirements PPRF-10..PPRF-14 of [safety.viz#99](https://github.com/jwildfire/safety.viz/issues/99) (the lab-family dock adoption, landing in batches), decomposed into atomic testable rows. Requirement IDs use the `PPRF-<AREA>-<NNN>` scheme in **base** form (`^[A-Z]{2,4}-[A-Z]+-\d+[A-D]?$`), matching the IDs tagged in the module's tests so each row resolves the matching `requirementIds` in the shipped `docs/evidence/participant-profile/evidence.json` by exact base-ID lookup. The `Evidence Type` column names the suite that evidences the row: `unit` → Vitest (`tests/unit/participant-profile/`), `browser` → Playwright (`tests/e2e/participant-profile.spec.js`, `tests/e2e/hep-explorer.spec.js`), `doc` → a documentation row evidenced by the named docs themselves (no test suite). Rollout rows (PPRF-OE-*, PPRF-DD-*, PPRF-SSP-*, PPRF-SH-*, PPRF-QT-*) are evidenced from the adopting renderer's own suites — `tests/unit/<renderer>/profile-adoption.test.js` and `tests/e2e/<renderer>.spec.js` (currently `outlier-explorer`, `delta-delta`, `shift-plot`, `histogram`, `qt-explorer`) — whose evidence pages resolve PPRF-* tags as IDs-only by established behavior. Rows carry Status `planned` until the implementation PR merges and `docs/participant-profile-coverage.md` lists them; the `Test/Evidence Link` points at the implementation issue until then.

## Requirement context

- Requirement: [obot.roadmap#45](https://github.com/jwildfire/obot.roadmap/issues/45) — participant-profile module (the "A+D hybrid" of the [design options report](https://jwildfire.github.io/obot.roadmap/reports/participant-profile-module-options-2026-07-22/))
- Implementation: [safety.viz#98](https://github.com/jwildfire/safety.viz/issues/98) — standalone renderer + dock, hep-explorer adopts first; `PPRF-HDR-003` additionally closes [safety.viz#53](https://github.com/jwildfire/safety.viz/issues/53) (templated `participantProfileURL` link-out)
- Prerequisite: [safety.viz#91](https://github.com/jwildfire/safety.viz/issues/91) — hep-core split (the module consumes `hep-core` reducers directly; design decision D4: no interim adapter)
- Rollout: [safety.viz#99](https://github.com/jwildfire/safety.viz/issues/99) — the lab-family adopters are IN scope here (rows per adopting batch; `results-over-time` defers to the shared selector, [safety.viz#87](https://github.com/jwildfire/safety.viz/issues/87)). Out of scope: exposure strip, AE/conmed tracks, drawer mode (v2 candidates per the design report); `Widget_ParticipantProfile` in gsm.safety
- Issue-level mapping: PPRF-1 → `PPRF-CORE-001..005`; PPRF-2 → `PPRF-HDR-001/002/003`; PPRF-3 → `PPRF-SPAG-001/002/003`; PPRF-4 → `PPRF-TBL-001..005`; PPRF-5 → `PPRF-STEP-001/002`; PPRF-6 → `PPRF-EVT-001/002`; PPRF-7 → `PPRF-HEP-001..005`; PPRF-8 → `PPRF-ACC-001`; PPRF-9 → `PPRF-GATE-001`; PPRF-10/11 → the per-adopter rollout areas (`PPRF-OE-001..003`, `PPRF-DD-001..004`, `PPRF-SSP-001..004` from Batch 1; `PPRF-SH-001..003`, `PPRF-QT-001..004` from Batch 2; the remaining adopter, `results-over-time`, defers to the shared selector, sv#87); PPRF-12 → `PPRF-DD-004` (bespoke measure table superseded); PPRF-13 → `PPRF-COV-001` (landed with Batch 2: ae-explorer / ae-timelines coverage-doc deferral notes); PPRF-14 → the Evidence Type / Test-Evidence Link columns of the rollout rows

## Source inventory

- `SafetyGraphics/hep-explorer` `src/callbacks/onResize/customizePoints/` — the original drill-down composition: `addPointClick`, `participantHeader/`, `spaghettiPlot/`, `measureTable/` (with `sparkLine/` + `lineChart/`)
- [Design options report](https://jwildfire.github.io/obot.roadmap/reports/participant-profile-module-options-2026-07-22/) — option analysis and decisions D1–D4 behind the A+D hybrid
- safety.viz module: `src/participant-profile.js` (standalone entry), `src/participant-profile/` (header, spaghetti, measure table, stepper, dock wiring), `src/shell.js` (`sv-profile` dock slot), `src/data/schema/participant-profile.json`
- safety.viz hep-explorer adoption: `src/hep-explorer.js` + `src/hep-explorer/views/{scatter,composite}.js` (dock on by default, bespoke `drawDetail` deleted), `src/hep-core/` reducers (`participantMeasureSeries`, `measureSummary`, quadrant/severity ordering)
- safety.viz tests: `tests/unit/participant-profile/`, `tests/unit/hep-explorer/profile-adoption.test.js`, `tests/e2e/participant-profile.spec.js`, `tests/e2e/hep-explorer.spec.js`
- safety.viz coverage doc: `docs/participant-profile-coverage.md`

## Requirements

| ID | Area | Requirement | Source | Evidence Type | Test/Evidence Link | Status | AI Review | Notes |
|---|---|---|---|---|---|---|---|---|
| PPRF-CORE-001 | CORE | The participant profile is a single module in src/participant-profile/ containing no renderer-specific logic; both mounts render the same header, spaghetti and measure-table composition from the same cleaned-row input. | sv#98 PPRF-1 \| design report §Option A+D \| safety.viz src/participant-profile/ | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | One module, two mounts: the dock and the standalone entry share one render path. |
| PPRF-CORE-002 | CORE | SafetyViz.participantProfile(el, data, config) renders the profile standalone, ingesting the standard long-lab data contract. | sv#98 PPRF-1 \| safety.viz src/participant-profile.js; src/data/schema/participant-profile.json | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The standalone mount is a full renderer with its own schema and ingest. |
| PPRF-CORE-003 | CORE | When docked, the profile renders into an sv-profile shell slot below the host chart card and is fed the host chart's cleaned rows, performing no second data ingest. | sv#98 PPRF-1 \| safety.viz src/shell.js (sv-profile slot); src/participant-profile/ dock wiring | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Dock mount reuses the host's cleaned rows — a single ingest per page. |
| PPRF-CORE-004 | CORE | The standalone mount renders the house shell chrome — sidebar controls with the chart card hidden, the profile block owning the main column — and shows an idle note naming the listen target until a selection arrives. | sv#98 PPRF-1 \| safety.viz src/shell.js; src/participant-profile.js renderChrome/setIdle | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The standalone renderer is a full shell citizen; the idle note tells integrators where the module is listening. |
| PPRF-CORE-005 | CORE | profileDock(container, settings) mounts the dock imperatively: it consumes the host's pre-cleaned rows verbatim — no checkInputs, no cleanData — installs no event listener, and is driven via show/clear/resize/destroy. | sv#98 PPRF-1 \| safety.viz src/participant-profile.js profileDock | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The dock contract: a single ingest per page, the host drives the lifecycle. |
| PPRF-HDR-001 | HDR | The profile header shows the participant id, the demographic fields configured in config.details, the computed R Ratio, and P_ALT where computable. | sv#98 PPRF-2 \| SafetyGraphics/hep-explorer src/callbacks/onResize/customizePoints/participantHeader/ | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Parity with the original participantHeader plus the computed hepatic stats. |
| PPRF-HDR-002 | HDR | The header provides a Clear affordance that empties the profile and, when docked, clears the host chart's selection highlight. | sv#98 PPRF-2 \| safety.viz src/participant-profile/ header | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Clear is the profile's only outbound action on the host — it resets selection state, it does not dispatch selection events (see PPRF-EVT-001). |
| PPRF-HDR-003 | HDR | When config.participantProfileURL is set, the header renders a link-out whose href substitutes the participant id for the {id} template token; no link renders when the setting is absent. | sv#98 PPRF-2 \| [sv#53](https://github.com/jwildfire/safety.viz/issues/53) (templated link-out superseding the original's static URL) | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Closes sv#53: the {id} template replaces the original renderer's static profile URL. |
| PPRF-SPAG-001 | SPAG | The labs-over-time spaghetti draws one day-indexed line per key measure, toggling between ×ULN and ×baseline display, with non-key measures available behind a toggle. | sv#98 PPRF-3 \| SafetyGraphics/hep-explorer src/callbacks/onResize/customizePoints/spaghettiPlot/ | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Day-indexed x-axis; display toggle mirrors the host explorer's eDISH/mDISH standardizations. |
| PPRF-SPAG-002 | SPAG | Each spaghetti measure applies its per-measure reference cut: the cut line appears on hover/focus and points at or above the cut render filled while points below render unfilled. | sv#98 PPRF-3 \| SafetyGraphics/hep-explorer spaghettiPlot reference lines \| safety.viz hep-core cuts | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Per-measure cuts, not a single global reference line. |
| PPRF-SPAG-003 | SPAG | Each measure receives a stable categorical palette color shared by its spaghetti line, sparkline and inset, cycling when a profile carries more measures than colors. | sv#98 PPRF-3 \| safety.viz src/participant-profile/configure.js MEASURE_COLORS/measureColorScale | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | One color per measure across every panel of the profile. |
| PPRF-TBL-001 | TBL | The measure table lists every measure — key measures first, extras behind a "show N additional" toggle — with N, min, median, max and an inline sparkline per row. | sv#98 PPRF-4 \| SafetyGraphics/hep-explorer src/callbacks/onResize/customizePoints/measureTable/ (with sparkLine/) | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Full parity with the original's table (design decision D2); summary stats via hep-core measureSummary. |
| PPRF-TBL-002 | TBL | Activating a row's sparkline expands an inset line chart showing the LLN–ULN normal-range band, population-extent guides defaulting to the 1st/99th percentiles, and marked outliers. | sv#98 PPRF-4 \| SafetyGraphics/hep-explorer measureTable/lineChart/ | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The inset is the original's expandable line chart with the normal-range band and population guides. |
| PPRF-TBL-003 | TBL | The inset line chart computes a padded y-domain unioning the participant's values, the population extent and the finite normal-range limits, and paints the LLN–ULN band and dashed population-extent guides in chart space with outlier points filled. | sv#98 PPRF-4 \| SafetyGraphics/hep-explorer measureTable/lineChart/ (setDomain, drawNormalRange, drawPopulationExtent, updatePointFill) | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The unit-level decomposition of the PPRF-TBL-002 inset: domain, band, guides and point fill each pinned numerically. |
| PPRF-TBL-004 | TBL | The extras toggle uses the original's "Show N additional measure(s):" copy, reveals and re-hides the non-key measure rows, and collapses any open extra-row inset when extras are hidden. | sv#98 PPRF-4 \| SafetyGraphics/hep-explorer measureTable/addExtraMeasureToggle.js | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Parity toggle copy; hiding extras never leaks an open inset chart. |
| PPRF-TBL-005 | TBL | The optional participant record listing (listing: true) renders the participant's rows through the shared listing renderer with columns derived from the lab mapping by default, a listing_cols override, and paging. | sv#98 PPRF-4 \| safety.viz src/histogram/listing.js (shared listing renderer) | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Optional block below the measure table; off by default. |
| PPRF-STEP-001 | STEP | When the selection holds more than one participant, the dock collapses to a stepper (◀ k of N · id ▶) that renders the full profile for the current participant and keeps the host chart highlight in sync while stepping. | sv#98 PPRF-5 \| design report §cohort stepper | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | New capability beyond the original renderer, which only handled single-participant drill-down. |
| PPRF-STEP-002 | STEP | The stepper orders participants worst quadrant first, falling back to peak severity where quadrant classification does not apply. | sv#98 PPRF-5 \| safety.viz src/hep-core/quadrants.js (concern ordering) | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Worst-first review order; severity fallback keeps the ordering defined for non-quadrant hosts. |
| PPRF-EVT-001 | EVT | Docked, the module derives its content from the host chart's selection state including clear; standalone, it listens for participantsSelected (payload {detail:{data: ids}}) on a configurable target; in neither mount does it dispatch selection events itself. | sv#98 PPRF-6 \| safety.viz participantsSelected event contract (selection.js writers) | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The profile is a pure consumer of selection — chart modules remain the only participantsSelected writers. |
| PPRF-EVT-002 | EVT | setSelected() takes the same path as the participantsSelected listener, and outbound coordination is callbacks only: stepper navigation reports through on_step and Clear through on_clear — the module dispatches no selection events. | sv#98 PPRF-5/6 \| safety.viz src/participant-profile.js setSelected/handleClear/step | unit | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The programmatic API mirrors the event contract; hosts hear callbacks, never events. |
| PPRF-HEP-001 | HEP | hep-explorer enables the dock by default in both views: scatter click keeps the on-chart visit trace and rugs while the module replaces the bespoke drawDetail, which is deleted. | sv#98 PPRF-7 \| SafetyGraphics/hep-explorer addPointClick \| safety.viz src/hep-explorer.js | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | First adopter; the on-chart visit trace + rugs stay with the chart, the drill-down moves to the module. |
| PPRF-HEP-002 | HEP | In the composite view, focusing a single participant (click or selector) opens the docked profile and a multi-participant selection shows the stepper. | sv#98 PPRF-7 \| safety.viz src/hep-explorer/views/composite.js | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Answers the collaborator question that triggered obot.roadmap#45: click a composite point, the story opens below. |
| PPRF-HEP-003 | HEP | In the composite view, focusing a single participant — point click or the shared Participants selector — opens the full docked profile, not the stepper. | sv#98 PPRF-7 \| safety.viz src/hep-explorer/views/composite.js | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Single focus is a full profile; the stepper is reserved for N > 1 cohorts (PPRF-HEP-002). |
| PPRF-HEP-004 | HEP | The migration ribbon hand-off arrives in the composite view with the dock opened on the carried cohort, with no dock-specific edits to the migration, composite or selection code paths. | sv#98 PPRF-7 \| safety.viz src/hep-explorer/views/migration.js (carried-selection hand-off) | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The §1.5 acceptance demo: carried ids flow through the existing selection choke point and the dock simply hears the dispatch. |
| PPRF-HEP-005 | HEP | A background click (empty selection dispatch) clears the selection, empties the dock slot, and the shell's :empty rule hides the block. | sv#98 PPRF-7 \| safety.viz src/shell.js (sv-profile :empty rule) | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Clearing restores the nine non-adopting renderers' layout math — an empty slot takes no space. |
| PPRF-OE-001 | OE | outlier-explorer enables the dock by default (config-on): the shared participant profile mounts in the shell profile slot with the host lab mappings, optional measure_values map, and profile_details passed through, and its feed rows are derived ONCE per data load through the shared hep-core cleaners — never per gesture. | sv#99 PPRF-10 \| safety.viz src/outlier-explorer.js; src/profile-host.js | unit | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | First rollout adopter alongside delta-delta (Batch 1); the shared host kit (src/profile-host.js) is host code, not module code. |
| PPRF-OE-002 | OE | Clicking a point feeds the docked profile the full participant story via the participantsSelected dispatch on the shell root (SOE-API-003), SUPPLEMENTING the linked listing, which stays; the dock Clear affordance routes through the host clear path and empties both. | sv#99 PPRF-10/11 \| safety.viz src/outlier-explorer.js selectParticipant/dispatchSelection | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | Linked listings stay (records vs story); the chart gesture is unchanged. |
| PPRF-OE-003 | OE | A background click empties the dock, and any control-driven redraw resets the selection and the dock in the same render preamble — the dock and the chart never disagree about what is selected. | sv#99 PPRF-11 \| safety.viz src/outlier-explorer.js render preamble | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | The render-preamble reset rule shared by every rollout adopter whose render clears selection silently. |
| PPRF-DD-001 | DD | delta-delta dispatches the house participantsSelected event (payload {detail:{data: ids}}, bubbling) on the shell root for point clicks AND clears — closing the renderer’s missing-dispatch gap (sv#88 SELN-4) as the dock prerequisite; the dispatch fires whether or not the dock is enabled. | sv#99 PPRF-10 \| sv#88 SELN-4 \| safety.viz src/delta-delta.js dispatchSelection | unit | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | The event contract is independent of the dock: profile: false still dispatches. |
| PPRF-DD-002 | DD | Clicking a delta-delta point opens the docked FULL profile: the underlying long-lab rows are cleaned once per data load (the baseline-vs-comparison delta is not re-encoded — the profile shows the full series), the point is border-highlighted, and the header details default to the host details minus the participant id. | sv#99 PPRF-10/11 \| safety.viz src/delta-delta.js profileSettings | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | The full-series story is the supersession argument for PPRF-DD-004. |
| PPRF-DD-003 | DD | delta-delta gains a clear gesture: an empty-canvas click restores the point borders, resets the annotation, and dispatches the empty selection so the dock empties; the dock Clear affordance and every control-driven redraw take the same clear path. | sv#99 PPRF-11 \| safety.viz src/delta-delta.js clearSelection + render preamble | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | New gesture — the renderer previously ignored empty clicks entirely. |
| PPRF-DD-004 | DD | The bespoke per-measure detail table (src/delta-delta/listing.js) is deleted in the adopting change: the docked profile supersedes it as the sole detail view, and the superseded matrix rows (SDD-FUNC-006 table portion, SDD-REG-011/014/016..025) are marked superseded in safety-delta-delta.md. | sv#99 PPRF-12 \| safety.viz src/delta-delta.js (listing.js removed) | unit | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | PPRF-12: partial bespoke detail the profile supersedes is removed in the adopting PR; SDD-REG-012/013 stay, retargeted at chart border + dock. |
| PPRF-SSP-001 | SSP | A multi-participant brush collapses the docked profile to the worst-first cohort stepper ("1 of N", module ranking), and stepper navigation border-emphasizes the stepped point on the chart transiently — without touching the brush selection and without re-dispatching. | sv#99 PPRF-10/11 \| safety.viz src/shift-plot.js showSelection/emphasizeParticipant | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | The rollout's stepper renderer: shift-plot is the only Batch-1/2 adopter whose gesture routinely selects N>1. |
| PPRF-SSP-002 | SSP | shift-plot enables the dock by default (config-on): a single-point brush shows the FULL docked profile (no stepper) beside the retained linked listing, with feed rows derived ONCE per data load from the retained rawData — never from the pair-per-participant chartPairs — through the shared hep-core cleaners, and visitn_col mapped from the host's visit_order_col. | sv#99 PPRF-10/11 \| safety.viz src/shift-plot.js profileSettings/buildProfileRows | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | Linked listings stay (records vs story); host `details` are pair columns, so header demographics come from profile_details only. |
| PPRF-SSP-003 | SSP | Clearing the selection — the tiny-click path, the dock Clear affordance (routed through the host clear path), or any control-driven re-render — empties the dock and restores the uniform point emphasis, with the empty dispatch travelling the same participantsSelected pipe. | sv#99 PPRF-11 \| safety.viz src/shift-plot.js clearSelection/render preamble | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | The render-preamble reset rule shared by every rollout adopter whose render clears selection silently. |
| PPRF-SSP-004 | SSP | The participantsSelected dispatch target moves from the host element to the shell root so the docked feed hears every dispatch (SSP-API-003 alignment with the house contract); the event still bubbles, so element-level listeners — the original SSP-API-003 surface — keep working. | sv#99 PPRF-10 \| safety.viz src/shift-plot.js dispatchSelected | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | Reworded SSP-API-003 in safety-shift-plot.md accordingly (shell root, bubbling to the wrapper). |
| PPRF-SH-001 | SH | histogram enables the dock by default (config-on): the shared participant profile mounts in the shell profile slot with the host lab mappings, optional measure_values map, and profile_details passed through, and its feed rows are derived ONCE per data load through the shared hep-core cleaners — never per gesture. | sv#99 PPRF-10 \| safety.viz src/histogram.js profileSettings/buildProfileRows; src/profile-host.js | unit | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | Batch-2 adopter alongside qt-explorer; reuses the Batch-1 shared host kit unchanged. |
| PPRF-SH-002 | SH | Clicking (or keyboard-activating) a row of the shared linked listing focuses that participant — a NEW host selection state (state.selectedId) dispatches the house participantsSelected event on the shell root, opening the docked FULL profile while the listing stays with the participant's rows highlighted; the row affordance is presence-of-callback opt-in (onListingRowClick), leaving the other shared-listing consumers byte-identical. | sv#99 PPRF-10/11 \| safety.viz src/histogram.js selectParticipant; src/histogram/listing.js opt-in rows | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | The histogram's participant surface is the listing, not the chart (bins aggregate); rows meet the PPRF-ACC-001 keyboard bar (role=button, tabindex, Enter/Space). Row highlight and dock never disagree. |
| PPRF-SH-003 | SH | Clearing the focus — the dock Clear affordance (routed through the host clear path, retaining the listing), a NEW bin click (which replaces the listing), or any control-driven re-render — empties the dock, un-highlights the rows, and dispatches the empty selection. | sv#99 PPRF-11 \| safety.viz src/histogram.js clearSelection/showListing/render preamble | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | The render-preamble reset rule shared by every rollout adopter whose render clears selection silently. |
| PPRF-QT-001 | QT | qt-explorer enables the dock by default (config-on) and gains a minimal outlier-scatter point-click selection (state.selectedId, single-select only): a point click emphasizes the point and dispatches the house participantsSelected event on the shell root, opening the docked FULL profile; no listing exists and none is added — the dock is the sole drill-down surface. | sv#99 PPRF-10/11 \| safety.viz src/qt-explorer.js selectParticipant/dispatchSelection | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | Batch-2 adopter alongside histogram; the scatter is the renderer's only per-participant surface. |
| PPRF-QT-002 | QT | The profile's long-lab contract is mapped for interval (ECG) measures: feed rows synthesize a unit ULN (__qt_profile_uln = 1) so the spaghetti renders OBSERVED milliseconds and nothing drops on the ULN guard; the identity measure_values map over the host's measures makes the ECG parameters the KEY measures; per-QTc cuts carry the first absolute threshold (450 ms default) with a NaN defaults entry leaving Heart Rate cut-free; the host's BASE value column is NOT passed as the profile's baseline flag. | sv#99 PPRF-10 \| safety.viz src/qt-explorer.js profileSettings/buildProfileRows | unit | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | "Interval measures render with their own cuts" (#99 PPRF-10); the 30/60 ms change thresholds are not representable in the dock — documented in docs/qt-explorer-coverage.md. |
| PPRF-QT-003 | QT | An empty-canvas click clears the selection and empties the dock, and every render — including a view switch — resets both in the render preamble; on the central/categorical views (no participant marks) the dock idles, still mounted. | sv#99 PPRF-11 \| safety.viz src/qt-explorer.js clearSelection/render preamble | browser | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | View coverage documented in docs/qt-explorer-coverage.md — only the outlier scatter carries per-participant marks. |
| PPRF-QT-004 | QT | The gesture is single-select only, but the dock honors the full contract for externally-fed cohorts: a root-level multi-id dispatch opens the worst-first stepper, stepping emphasizes the stepped point without re-dispatching, and dock Clear empties the cohort with the empty dispatch travelling the same pipe. | sv#99 PPRF-11 \| safety.viz src/qt-explorer.js emphasizeParticipant + profile-host feed | unit | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | The shared-selector (#87) readiness story: the host never multi-selects, the dock still steps. |
| PPRF-COV-001 | COV | The non-lab renderers ae-explorer and ae-timelines carry a one-line coverage-doc note deferring their profile adoption to the AE-domain v2 sections — they ingest adverse-event records, not the measure-per-visit long-lab contract the dock consumes. | sv#99 PPRF-13 \| safety.viz docs/ae-explorer-coverage.md; docs/ae-timelines-coverage.md | doc | [sv#99](https://github.com/jwildfire/safety.viz/issues/99) | planned | Planned — see implementation issue. | Mirrors the sv#88 inventory convention; landed with Batch 2. Documentation row — evidenced by the coverage docs themselves. |
| PPRF-ACC-001 | ACC | The profile is keyboard-operable throughout — stepper arrows, sparkline expansion and Clear — with labeled regions, :focus-visible rings, and reduced-motion-safe transitions. | sv#98 PPRF-8 (bar set by sv#76/sv#87) | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | Matches the accessibility bar set by the selector (#87) and the earlier a11y pass (#76). |
| PPRF-GATE-001 | GATE | The standalone renderer ships the full done-gate: gallery card, live linked-charts demo, guide, evidence page, API reference and schema, with evidence.json refreshed and canonical Linux baselines regenerated. | sv#98 PPRF-9 \| renderer done-gate convention (hub#21) | browser | [sv#98](https://github.com/jwildfire/safety.viz/issues/98) | planned | Planned — see implementation issue. | The linked-charts demo doubles as the PPRF-EVT-001 standalone-wiring exhibit; this matrix is the companion-PR half of the gate. |
